{% extends 'RentBundle::base.html.twig' %}

{% block headline %}Konfiguration aktualisieren{% endblock %}

{% block content %}

<form action="{{ path('configuration_update', {'id': config.id}) }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
	{{ form_errors(form) }}
	{{ form_widget(form) }}

	<div class="form-actions">
    	<input class="btn btn-primary" type="submit" value="Konfiguration aktualisieren" />
		<a href="{{ path('configuration_index') }}" class="btn">Abbrechen</a>
	</div>
</form>

{% endblock %}
